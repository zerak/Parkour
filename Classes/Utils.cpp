#include "Utils.h"
#include "resource.h"

USING_NS_CC;

/**
 * format an integer to a specific length
 * @param {int}
 * @param {int}
 */
#if 0
//将一个将整数转为一个特定长度
int FormatNumberLength(int num, int length) {
    int r = "" + num;
    while (r.length < length) {
        r = "0" + r;
    }
    return r;
}
#else


#endif

